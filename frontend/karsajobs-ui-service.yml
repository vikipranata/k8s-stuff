apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui-service
spec:
  selector:
    app: karsajobs-ui
  type: NodePort
  ports:
    - name: karsajobs-ui
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30808